import { createContext } from "react";
export const UserContext = createContext();//Created Context (Global Variable),createContext return object 
//UserContext = Context Object (Provider + Consumer) 
//PascalCase naming Convention, not a component.
